{% extends 'base_admin.html.twig' %}

{% block body %}
Accueil ADMIN

    {% set prestataires =  render(controller('AppBundle:Admin\\APrestataire:jsonList'))|json_decode %}

    {{ dump(prestataires) }}
    {% for prestataire in prestataires %}
        {{ dump(prestataire) }}
    {% endfor %}
{% endblock %}
