{% extends 'base_admin.html.twig' %}

{% block document_title_h1 %}Gestion des {{ type }} {% endblock %}

    {% block document_title_list %}
        <h2 class="page-title">{{ promotion.nom }}: detail</h2>
    {% endblock %}

{% block body %}

    <div class="background-white p30 mb50 container">
        {#<div class="grid">#}
            <div class="col-sm-4">
                    <div class="card-system">
                        <h2>
                            <a href="{{ path('admin_promotion_detail', {'slug': promotion.slug}) }}">
                                {{ promotion.nom }}
                            </a>
                        </h2>
                        <div class="card-system-inner">
                            {#<div class="card-system-image"#}
                                 {#data-background-image="{{ promotion.image.url }}"#}
                                 {#style="background-image: url({{ promotion.image.url }});">#}
                                {#<a href="{{ path('admin_promotion_detail', {'slug': promotion.slug}) }}"> </a>#}
                            {#</div><!-- /.card-system-image -->#}

                            <div class="card-system-content">

                                <p>
                                    <a href="#" class="btn btn-primary btn-md  btn-block">Visuel</a>
                                            <a href="#" class="btn btn-secondary btn-md btn-block ">Invalider</a>
                                    <a href="#" class="btn btn-secondary btn-block">message</a>
                                    <a href="#" class="btn btn-danger btn-block">Supprimer</a>
                                </p>
                            </div>
                        </div>
                    </div>
            </div>
            <div class="col-sm-8">
                <div class="card-simple">
                        <h2 class="header"> Description:</h2>
                            <p>
                                {{ promotion.description }}
                            </p>
                    <div class="post-meta-tags clearfix">
                        <h2 class="header">  Proposé par:</h2>
                        <ul>
                            <li class="tag">
                                <a href="#">
                                    {{promotion.prestataire.nom}}
                                </a>
                            </li>
                        </ul>
                    </div>

                </div>
            </div>
        </div>
    {#</div>#}
{% endblock %}

